{% macro hint(value, inputName) %}
  <small class="hint" for="{{ inputName }}">{{ value }}</small>
{% endmacro %}

{% macro input(name, value, type, attributes) %}
  <input name="{{ name }}" type="{{ type }}" value="{{ value }}"{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}{% if not attributes.id is defined %} id="{{ name }}"{% endif %}/>
{% endmacro %}

{% macro label(id, content, attributes) %}
  <label for="{{ id }}"{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}>{{ content|e }}</label>
{% endmacro %}

{% macro text(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "text", attributes) }}
  </formset>
{% endmacro %}

{% macro password(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "password", attributes) }}
  </formset>
{% endmacro %}

{% macro email(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "email", attributes) }}
  </formset>
{% endmacro %}

{% macro color(name, value, attributes) %}
  {% from _self import input %}
  <formset class="form-group">{{ input(name, value, "color", attributes) }}</formset>
{% endmacro %}

{% macro button(text, class, id, value, attributes) %}
  <button class="button {{ class }}"{% if id is defined%} id="{{ id }}"{% endif %}{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}>{{ text | raw }}</button>
{% endmacro %}

{% macro date(name, displayLabel, value, attributes) %}
{% from _self import input %}
{% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "date", attributes) }}
  </formset>
{% endmacro %}

{% macro file(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "file", attributes) }}
  </formset>
{% endmacro %}

{% macro hidden(name, value, attributes) %}
  {% from _self import input %}
  {{ input(name, value, "hidden", attributes) }}
{% endmacro %}

{% macro number(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "number", attributes) }}
  </formset>
{% endmacro %}

{% macro range(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "range", attributes) }}
  </formset>
{% endmacro %}

{% macro search(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "search", attributes) }}
  </formset>
{% endmacro %}

{% macro tel(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "tel", attributes) }}
  </formset>
{% endmacro %}

{% macro time(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    {{ input(name, value, "time", attributes) }}
  </formset>
{% endmacro %}

{% macro url(name, displayLabel, value, attributes) %}
  {% from _self import input %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
  {{ input(name, value, "url", attributes) }}
  </formset>
{% endmacro %}

{% macro textarea(name, displayLabel, value, attributes) %}
  {% from _self import label %}
  <formset class="form-group">
    {{ label(name, displayLabel) }}
    <textarea name="{{ name }}"{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}{% if not attributes.id is defined %} id="{{ name }}"{% endif %}>{{ value|e }}</textarea>
  </formset>
{% endmacro %}

{% macro checkbox(name, displayLabel, value, id, attributes) %}
  <div class="checkbox"><input type="checkbox" name="{{ name }}" id="{{ id|default(name) }}" class="{{ class }}" value="1"{% if value %} checked="checked"{% endif %}{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %} />
  <i class="fas fa-circle"></i><i class="fas fa-check-circle"></i>
  <label for="{{ name }}" >{{ displayLabel }}</label></div>
{% endmacro %}

{% macro select(name, values, value, attributes, displayLabel) %}
    <formset class="form-group">
  {% if displayLabel %}<label for="{{name}}">{{displayLabel}}</label>{% endif %}
  <select name="{{ name }}"{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}{% if not attributes.id is defined %} id="{{ name }}"{% endif %}>
    {% for key, name in values %}
    <option value="{{ key }}"{% if value == key %} selected="selected"{% endif %}>{{ name }}</option>
    {% endfor %}
  </select>
  </formset>
{% endmacro %}

{% macro radio(name, values, value, attributes) %}
  {% for key, label in values %}
  <label{% if value == key %} class="selected"{% endif %} ><input type="radio" name="{{ name }}" value="{{ key }}"{% if value == key %} checked="checked"{% endif %}{% for attr, value in attributes %} {{ attr }}="{{ value }}"{% endfor %}{% if not attributes.id is defined %} id="{{ name }}"{% endif %}> {{ label }}</label>
  {% endfor %}
{% endmacro %}